#include<stdio.h>
void main()
{
    int a,b,k;
    for(b=1;b<=5;b++)
    {
        for(k=0;k<=5-b;k++)
    {
        printf(" ");
    }
    for(a=5;a>=5-b+1;a--)
    {
        printf("%d",a);
    }
    printf("\n");
    }
}